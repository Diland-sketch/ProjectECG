<UserControl x:Class="GUI.View.UserControlConsultaMedico"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Opacity="1" Loaded="UserControl_Loaded">
    <Grid Width="598" Height="456">
        <Grid.Background>
            <ImageBrush ImageSource="/View/blackMedicalBackground.jpg"/>
        </Grid.Background>
        <Button x:Name="btnBack" HorizontalAlignment="Left" VerticalAlignment="Top" Click="btnBack_Click" Height="31" Width="32">
            <Button.Content>
                <Path Data="M 0 0 L 1 1 L 2 0" Stroke="Black" StrokeThickness="2" Fill="Transparent" Width="11" Height="9" Stretch="Fill" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="90"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Path.RenderTransform>
                </Path>
            </Button.Content>
        </Button>
        <ListView x:Name="MedicosListView" Margin="128,74,128,10" SelectionChanged="MedicosListView_SelectionChanged" Foreground="Black" Background="White">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Identificacion" DisplayMemberBinding="{Binding Identificacion}" Width="100"/>
                    <GridViewColumn Header="PrimerNombre" DisplayMemberBinding="{Binding PrimerNombre}" Width="150"/>
                    <GridViewColumn Header="Sexo" DisplayMemberBinding="{Binding Sexo}" Width="50"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Label Content="CONSULTA MEDICO" HorizontalAlignment="Center" Margin="0,27,0,0" VerticalAlignment="Top" FontSize="16" Foreground="White"/>

    </Grid>
</UserControl>

