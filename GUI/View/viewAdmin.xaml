<Window x:Class="GUI.View.viewAdmin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI.View"
        mc:Ignorable="d"
        Title="viewAdmin" Height="450" Width="800" Background="Black"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowState="Maximized"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Background="#272B2F">
            <Grid>
                <Label x:Name="lblMedico" Content="GESTION MEDICO" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0" FontSize="16" Cursor="Hand" MouseDown="Label_MouseDown" Foreground="White"/>
                <Label x:Name="lblRol" Content="GESTION ROL" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="16" Margin="0,50,0,0" Cursor="Hand" MouseDown="Label_mouse" Foreground="White"/>
                <Button Content="Back" Margin="0,366,0,0" Width="70" Background="#FF333838" Click="Button_Back" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                
            </Grid>
        </Border>
        <Border Grid.Column="1" Background="#212121">
            <Grid x:Name="panelMedico" Cursor="">
                
            </Grid>
        </Border>
    </Grid>
    <!--<DockPanel LastChildFill="False" Margin="0,0,0,-6">
        <DockPanel.Background>
            <ImageBrush/>
        </DockPanel.Background>
        <StackPanel Width="203" Background="#212121">
            <Label x:Name="lblMedico" Content="GESTION MEDICO" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0" FontSize="16" Cursor="Hand" MouseDown="Label_MouseDown" Foreground="White"/>
            <Label x:Name="lblRol" Content="GESTION ROL" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="16" Margin="0,50,0,0" Cursor="Hand" MouseDown="Label_mouse" Foreground="White"/>
            <Button Content="Back" Margin="0,70,0,0" Width="71" Background="#FF333838" Click="Button_Back" Foreground="White"/>
        </StackPanel>
        <StackPanel x:Name="panelMedico" DockPanel.Dock="Right" Width="598" Background="#212121"/>        
    </DockPanel>-->
</Window>
