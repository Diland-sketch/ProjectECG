<Window x:Class="GUI.View.viewAdmin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI.View"
        mc:Ignorable="d"
        Title="viewAdmin" Height="450" Width="800" Background="Black"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowState="Maximized"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Background="#272B2F">
            <Grid>
                <Label x:Name="lblMedico" Content="GESTION MEDICO" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Margin="0,10,0,408" FontSize="16" Cursor="Hand" MouseDown="Label_MouseDown" Foreground="White" Width="250"/>
                <Label x:Name="lblRol" Content="GESTION ROL" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="16" Margin="0,64,0,351" Cursor="Hand" MouseDown="Label_mouse" Foreground="White"/>
                <Button Content="Back"  Background="#FF333838" Click="Button_Back" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" Height="34" Width="48" Margin="0,387,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#212121">
            <Grid Name ="panelMedico">

            </Grid>
        </Border>


    </Grid>
    <!--<DockPanel LastChildFill="False" Margin="0,0,0,-6">
        <DockPanel.Background>
            <ImageBrush/>
        </DockPanel.Background>
        <StackPanel Width="203" Background="#212121">
            <Label x:Name="lblMedico" Content="GESTION MEDICO" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Margin="0,100,0,0" FontSize="16" Cursor="Hand" MouseDown="Label_MouseDown" Foreground="White"/>
            <Label x:Name="lblRol" Content="GESTION ROL" HorizontalContentAlignment="Center" FontSize="16" Margin="0,50,0,0" Cursor="Hand" MouseDown="Label_mouse" Foreground="White"/>
            <Button Content="Back" Margin="0,70,0,0" Width="71" Background="#FF333838" Click="Button_Back" Foreground="White"/>
        </StackPanel>
        <StackPanel x:Name="panelMedico" DockPanel.Dock="Right" Width="598" Background="#212121"/>
    </DockPanel>-->
</Window>